node_modules/
.env
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
licenses.db
licenses.db-*

# The built addon jar /download serves. Uploaded directly to the host, not
# committed — it's a binary build artifact, not source.
releases/

addon/.gradle/
addon/build/
addon/out/

# Raw compiled .class files from the source-recovery zip — redundant with
# the original jar, which you already have. Not committed here.
addon/original-jar-contents/
